Sources for the plugin `plugin.twitter`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.twitter" = {
            publisherId = "com.jasonschroeder",
        },
    },
}
```
